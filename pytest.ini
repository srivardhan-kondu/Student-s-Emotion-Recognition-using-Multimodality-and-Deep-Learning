[pytest]
pythonpath =
    src
    tests
testpaths = tests
addopts = -ra --tb=short --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    integration: loads TensorFlow / PyTorch / saved_models (slow)
